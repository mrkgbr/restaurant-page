// contact contains: whole contact page in main
const contact = () => {
  const contact = document.querySelector(".main");
  contact.textContent = "This is the main of contact";
};

export default contact;
